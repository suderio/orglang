---
trigger: always_on
---

We are designing a new programming language with a somewhat distinct syntax. It is better to not assume an error is due to implementation error. Sometimes it is better to ask if a construction in the org language is correct before trying to solve a bug in the implementation that may be in the org language code we are trying to compile. When in doubt, always ask if the org expression generating the error is corre