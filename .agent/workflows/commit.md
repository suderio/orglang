---
description: Automatically add and commit all changes with a descriptive message
---

// turbo
1. Run just clean
```bash
just clean
```

// turbo
2. Run git add and commit
```bash
git add --all && git commit -m "{{message}}"
```
